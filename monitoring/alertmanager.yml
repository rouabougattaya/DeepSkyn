route:
  receiver: devops-team
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 2h

receivers:
  - name: devops-team
    webhook_configs:
      - url: http://host.docker.internal:5001/alert
        send_resolved: true
